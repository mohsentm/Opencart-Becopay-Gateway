<link href="catalog/view/theme/stylesheet/becopay/checkout.css" rel="stylesheet" />

<div class="content">
    <form action="{{ action }}" method="post" id="wtp-checkout">
        <p>{{ text_chosen }}</p>
        <div class="buttons">
            <div class="pull-right">
                <button id="button-confirm"
                        class="btn btn-primary"
                        data-loading-text="{{ text_loading }}">
                    {{ button_confirm }}
                </button>
            </div>
        </div>
    </form>
</div>
